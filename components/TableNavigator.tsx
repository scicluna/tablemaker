type TableNavigatorProps = {
    direction: number
}

export default function TableNavigator({ direction }: TableNavigatorProps) {
    return (
        <h1 className="hidden sm:flex">Navigator</h1>
    )
}